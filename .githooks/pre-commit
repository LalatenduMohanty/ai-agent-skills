#!/usr/bin/env bash
# Pre-commit hook: ensure README.md skills tables are up to date.
# Install with: git config core.hooksPath .githooks

set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
"$REPO_ROOT/scripts/update-readme-skills.sh" --check
